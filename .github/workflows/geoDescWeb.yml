on:
  push:
    paths:    
    - 'website/geoDesc/**'
name: geoDesc Website Update

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy geoDesc Node
        uses: appleboy/ssh-action@v0.1.2
        with:
         host: ${{ secrets.SSH_HOST }}
         username: ${{ secrets.SSH_USERNAME }}
         password: ${{ secrets.SSH_PASSWORD }}
         script: |
               cd www
               git clone https://github.com/DanRunfola/gbRelease.git
               cd gbRelease
               git checkout gD
               cd website
               cd geoDesc
               #cp ~/geodesc.org/gbRelease/website/geoDesc/* ~/geodesc.org
               #cd ~/geodesc.org
               npm install
               export MapboxAccessToken=${{ secrets.MAPBOX }}
               npm start
